import { CoilValue } from "@alxshelepenok/diesel";
declare const useCoilPersistedState: <T>(coilValue: CoilValue<T>, keyPrefix?: string) => readonly [T, (nextValue: T) => void];
export default useCoilPersistedState;
