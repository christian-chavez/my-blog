import { SDK_VERSION, init as init$1 } from '@sentry/react';
import { getIntegrationsFromOptions } from './utils/integrations.js';

/**
 * Inits the Sentry Gatsby SDK.
 */
function init(options) {
  options._metadata = options._metadata || {};
  options._metadata.sdk = options._metadata.sdk || {
    name: 'sentry.javascript.gatsby',
    packages: [
      {
        name: 'npm:@sentry/gatsby',
        version: SDK_VERSION,
      },
    ],
    version: SDK_VERSION,
  };

  const integrations = getIntegrationsFromOptions(options);
  init$1({
    ...options,
    integrations,
  });
}

export { init };
//# sourceMappingURL=sdk.js.map
