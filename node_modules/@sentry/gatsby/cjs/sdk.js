Object.defineProperty(exports, '__esModule', { value: true });

const react = require('@sentry/react');
const integrations = require('./utils/integrations.js');

/**
 * Inits the Sentry Gatsby SDK.
 */
function init(options) {
  options._metadata = options._metadata || {};
  options._metadata.sdk = options._metadata.sdk || {
    name: 'sentry.javascript.gatsby',
    packages: [
      {
        name: 'npm:@sentry/gatsby',
        version: react.SDK_VERSION,
      },
    ],
    version: react.SDK_VERSION,
  };

  const integrations$1 = integrations.getIntegrationsFromOptions(options);
  react.init({
    ...options,
    integrations: integrations$1,
  });
}

exports.init = init;
//# sourceMappingURL=sdk.js.map
